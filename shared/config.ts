/**
 * ========================================
 * CENTRALIZED CONFIGURATION (Re-exports)
 * ========================================
 * 
 * This file re-exports from the centralized config files for backward compatibility.
 * 
 * MAIN CONFIG FILES (Edit these to make changes):
 * - /config/app.config.ts - Website and auction settings
 * - /config/teams.config.ts - Team colors, logos, and branding
 */

export * from '../config/app.config';
export * from '../config/teams.config';
